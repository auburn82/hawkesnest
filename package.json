{
  "name": "hawkesnest-streaming",
  "version": "1.0.0",
  "description": "Personal streaming portal for hawkenest.cc",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy dist"
  },
  "dependencies": {
    "hls.js": "^1.5.7"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
